{
  "name": "your-project",
  "version": "1.0.0",
  "scripts": {
    "build": "react-scripts build" // Replace with your framework's build command if different
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.x"
  }
}
